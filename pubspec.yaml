name: flutterappposts
description: Bloc y Firestore

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_bloc: ^4.0.0
  cloud_firestore: ^0.13.5
  firebase_storage: ^3.1.5
  equatable: ^1.1.1
  meta: ^1.1.8
  intl: ^0.16.1
  image_picker: ^0.6.6+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/